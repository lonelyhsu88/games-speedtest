# 快速開始指南

## ✅ 您的腳本狀態

### 舊版（仍可使用，但資料不準）
```bash
./test_games_menu.sh              # ❌ 使用舊版測速 (資料可能少 70%)
./test_games_with_puppeteer.sh    # ❌ 使用舊版測速
```

### 新版（推薦，資料準確）
```bash
./test_games_menu.sh        # ✅ 使用修復版測速 (準確)
./test_games_with_puppeteer.sh  # ✅ 使用修復版測速
```

---

## 🚀 立即開始使用修復版

### 方式一：使用互動式選單（最簡單）

```bash
./test_games_menu.sh
```

然後：
1. 選擇要測試的遊戲（輸入編號切換選擇）
2. 選擇語言 (en-US / zh-CN / zh-TW)
3. 選擇等待時間（建議選 2 = 15 秒）
4. 確認並開始測試

### 方式二：直接指定遊戲

```bash
# 測試特定遊戲
./test_games_with_puppeteer.sh --games "StandAloneLimboCL,EggHuntBingo" en-US 15000

# 測試 3 個隨機遊戲
./test_games_with_puppeteer.sh 3 en-US 15000
```

### 方式三：直接使用測速程式

```bash
# 測試單一遊戲 URL
node puppeteer_game_test.js "https://your-game-url" --output=report.json
```

---

## 📊 預期結果對比

### 舊版（不準確）
```
✓ StandAloneLimboCL      23.87s |   2.68 MB | 133 requests
  ❌ 資源大小少計算約 80%
  ❌ 請求數可能遺漏 30-40%
```

### 修復版（準確）
```
✓ StandAloneLimboCL      35.42s |  12.90 MB (10.85 MB transferred) | 209 requests
  ✅ 資源大小準確（與瀏覽器 DevTools 一致）
  ✅ 完整追蹤所有請求
  ✅ 顯示壓縮資訊
```

---

## 🔍 驗證修復是否成功

### 步驟 1: 使用修復版測試
```bash
./test_games_menu.sh
# 選擇 1 個遊戲測試
```

### 步驟 2: 同時開啟瀏覽器 DevTools
1. 打開同一個遊戲 URL
2. 開啟 DevTools (F12) → Network 標籤
3. 重新整理頁面
4. 等待完全載入

### 步驟 3: 對比結果
比較 **Requests 數量** 和 **Total Size**：
- ✅ 如果相差 < 5%，表示修復成功
- ❌ 如果差距很大，請回報問題

---

## 📁 輸出檔案位置

測試完成後，結果會儲存在：

```bash
results/
├── puppeteer_game_test_2025XXXX_XXXXXX/
│   ├── StandAloneLimboCL_XXXXXXXXX.json    # 詳細報告
│   ├── EggHuntBingo_XXXXXXXXX.json
│   └── results.txt                          # 簡要摘要
```

### 查看詳細報告
```bash
# 查看 JSON 報告
cat results/puppeteer_game_test_*/StandAloneLimboCL_*.json | jq '.'

# 查看摘要
cat results/puppeteer_game_test_*/results.txt
```

---

## ⚡ 常用命令

### 測試特定類型的遊戲

```bash
# 選單模式 - 按 'b' 選擇所有 Bingo 遊戲
./test_games_menu.sh
# 然後按 b

# 或直接指定
./test_games_with_puppeteer.sh --games "ArcadeBingo,BonusBingo,CaribbeanBingo" en-US 15000
```

### 快速測試（10 秒等待）
```bash
./test_games_with_puppeteer.sh --games "StandAloneHilo" en-US 10000
```

### 深度測試（20 秒等待）
```bash
./test_games_with_puppeteer.sh --games "StandAloneHilo" en-US 20000
```

---

## 🐛 疑難排解

### 問題：測試太慢

**原因**：修復版會等待更長時間以確保完整追蹤

**解決**：
```bash
# 減少等待時間（但可能遺漏部分資源）
./test_games_menu.sh
# 選擇 wait time = 1 (10 秒)
```

### 問題：記憶體不足

**解決**：
```bash
# 一次只測試 1-3 個遊戲
# 不要選擇 "a" (全選)
```

### 問題：某些遊戲測試失敗

**檢查**：
```bash
# 查看詳細錯誤
cat /tmp/puppeteer_output.txt

# 使用非 headless 模式查看
node puppeteer_game_test.js "URL" --headless=false
```

---

## 📋 測試建議

### 第一次使用
1. ✅ 先測試 1-2 個熟悉的遊戲
2. ✅ 對比瀏覽器 DevTools 驗證準確度
3. ✅ 確認結果符合預期

### 日常使用
1. 測試 3-5 個代表性遊戲
2. 使用標準等待時間 (15 秒)
3. 定期建立效能基準

### 效能測試
1. 選擇不同類型的遊戲 (Bingo/Hash/Arcade)
2. 測試不同網路環境 (VPN/Direct)
3. 記錄並比較歷史資料

---

## 🔄 從舊版遷移

如果您之前使用舊版：

### 不要直接比較歷史資料
```bash
# ❌ 錯誤做法
# 比較舊版的 2.68 MB 和新版的 12.9 MB
# 這兩個數字不可比較！

# ✅ 正確做法
# 使用新版重新建立基準
# 未來的測試都用新版進行對比
```

### 重新建立基準測試
```bash
# 1. 使用修復版測試所有重要遊戲
./test_games_menu.sh
# 選擇需要的遊戲

# 2. 記錄結果作為新的基準
cp results/puppeteer_game_test_latest/results.txt baseline_$(date +%Y%m%d).txt

# 3. 未來的測試與此基準比較
```

---

## ✅ 檢查清單

使用修復版前，確認：

- [ ] Node.js 和 Puppeteer 已安裝
- [ ] 有足夠的磁碟空間 (至少 1GB)
- [ ] 網路連線正常
- [ ] 測試用的 IP 已加入白名單
- [ ] 了解測試時間可能比舊版長 20-50%

開始測試：

- [ ] 執行 `./test_games_menu.sh`
- [ ] 選擇 1-2 個遊戲測試
- [ ] 對比瀏覽器 DevTools 驗證
- [ ] 確認結果準確度 < 5% 差距
- [ ] 建立效能基準供未來對比

---

## 🆘 需要協助？

1. 查看詳細文檔：
   - `README_REVIEW.md` - 總覽和問題摘要
   - `CODE_REVIEW_FINDINGS.md` - 詳細技術分析
   - `COMPARISON.md` - 舊版 vs 新版對比
   - `MIGRATION_GUIDE.md` - 遷移指南

2. 檢查測試日誌：
   ```bash
   cat /tmp/puppeteer_output.txt
   ```

3. 驗證腳本版本：
   ```bash
   head -n 5 test_games_menu.sh
   # 應該看到 "FIXED VERSION"
   ```

---

## 🎉 開始測試！

```bash
# 最簡單的方式
./test_games_menu.sh

# 享受準確的測速結果！
```
